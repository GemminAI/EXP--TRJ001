# PATCH NOTES — v1.2 → v1.3

Claude Code による実装レビューで発見された計画書の構造的欠落を修正する。
**実装側の判断では埋めず、計画書側を改訂する。**

## 発見された問題

1. §5.3 の 1,000 件はすべて `fictional_*`（Type A 用）テンプレートの内訳で埋まっており、
   §5.1/§5.2 で規定された Type B（実在概念に対する事実誤り）用のプロンプト予算が
   計画書のどこにも存在しない。
2. §5.3 の実在衝突チェックは「Wikipedia API および検索 API」を前提としていたが、
   Google Custom Search 相当の実装は組織内に見当たらなかった。フォールバック規定がない。

## 修正方針

- Type A の予算（1,000 件・5 テンプレート・mixed 率較正の対象）は変更しない。
  Type A は主解析対象であり、統計的検出力を落とさないため。
- **Type B 用に独立した別枠を新設する**（200 件）。Type A の mixed 率較正とは
  別集計とし、Phase 1 のゲート判定（§5.4）には算入しない。
- 実在衝突チェックは Wikipedia API 単独で実施すると明記し、検出範囲外の
  残存リスクを評価ログに記録する規定に差し替える。

---

## §5.3 プロンプト設計 の全文差し替え

### 旧（v1.2）

> **総数 1,000 件**。5 テンプレートに均等分散（各 200 件）。
>   - `fictional_movie` / `fictional_person` / `fictional_paper` / `fictional_chemical` / `fictional_location`
> - **実在衝突チェック**: Wikipedia API および検索 API の検出範囲で自動照合し、実在が疑われる固有名詞を除外。検出範囲外の残存リスクがあることを評価ログに明記する（「100% 検証」とは記載しない）。
> - **Leave-one-template-out 評価**を副次指標として実施（§7.3）。

### 新（v1.3）

**Type A 用プロンプト（主解析対象、1,000 件）**

5 テンプレートに均等分散（各 200 件）。

- `fictional_movie` / `fictional_person` / `fictional_paper` / `fictional_chemical` / `fictional_location`
- mixed 率較正（§5.4）の対象。Phase 1・Phase 2 の予算はこの 1,000 件から充当する。

**Type B 用プロンプト（副次解析対象、200 件・別枠）**

- 実在する概念（映画・人物・化学物質・地名等、Type A と対応するドメインを推奨）について、
  事実の正誤を問う質問を 200 件用意する。ドメイン内訳は Type A の 5 テンプレートに準拠し
  各 40 件を目安とする（総数維持を優先し、内訳の均等性は必須としない）。
- 正解集合（`configs/typeB_reference.json`）を Phase 0 で確定し、出典とスナップショット日時を記録する。
- **Type A の 1,000 件とは完全に独立した予算**であり、Phase 1 の mixed 率ゲート（§5.4）には算入しない。
  Type B は Phase 2 開始時に全件を一括採取し（8 サンプル/プロンプトの同一プロトコルを適用）、
  Phase 3 では Type A の主解析と並行して副次解析として報告する。
- Type B にはパイロット較正フェーズを設けない。プロンプト数が固定（200 件）であるため、
  mixed 率が低くても追加生成は行わない。有効陽性数が不足した場合はその旨を結果に明記し、
  検出力不足として扱う（Type A の Phase 2 逆算式による規模調整の対象外）。

**実在衝突チェック（Type A 用、Google API フォールバックの明記）**

- 実装: Wikipedia API（MediaWiki `action=query`、`missing` フラグ検出）による自動照合。
  レート制限対応クライアントは既存資産（組織内 `stage1_ingest.py` 相当）を再利用してよい。
- **Google Custom Search 等の第二照合手段は本実験では実施しない**（組織内に既存実装がなく、
  新規導入のコストが本実験のスコープに見合わないため）。
- 上記の帰結として、Wikipedia に記載のない実在エンティティ（マイナーな地名・非英語圏の
  人物等）を誤って架空と判定するリスクが残る。この残存リスクを評価ログ
  （`results/collision_check_log.md` 等）に明記し、「100% 検証」とは記載しない。
- Type B は実在確定済みの概念を選定する工程であり、本チェックの対象外。

**Leave-one-template-out 評価**

Type A の 5 テンプレートについて副次指標として実施（§7.3）。Type B はテンプレート横断
比較の対象としない。

---

## 波及する修正（他節）

### §2.5 凍結対象チェックリスト

以下を追加。

- [ ] Type B 用プロンプト 200 件（`configs/prompts_typeB_v1.json`）
- [ ] Type B 正解集合・出典・スナップショット日時（`configs/typeB_reference.json`）

### freeze/FREEZE_MANIFEST.md

成果物一覧に追加。

| # | 成果物 | パス | SHA-256 |
|---|---|---|---|
| 12 | Type B 用プロンプト 200 件 | `configs/prompts_typeB_v1.json` | _未記入_ |
| 13 | Type B 正解集合 | `configs/typeB_reference.json` | _未記入_（既存 #7 と重複していた場合は統合） |

### CHANGELOG.md への追記

```
## v1.3（凍結候補・未凍結）

- **T1** Type B 用プロンプトの予算欠落を修正。v1.2 では 1,000 件の全枠が
  Type A（fictional_*）で占められ、§5.1/§5.2 で規定した Type B に対応する
  プロンプト予算が存在しなかった。Type A とは独立した別枠として 200 件を新設し、
  Phase 1 の mixed 率ゲートには算入しないと明記。
- **T2** 実在衝突チェックを Wikipedia API 単独に確定。Google Custom Search 相当の
  実装が組織内に見当たらなかったため、フォールバックを設けず、検出範囲外の
  残存リスクを評価ログに記録する規定に差し替え。
```

---

## この改訂の位置づけ

v1.2 から v1.3 への変更は、実験の統計的性格（H0・H2・Gate 3・停止規則）には触れていない。
Type A の予算・mixed 率ロジック・Gate 3 の判定条件はすべて不変。変更は Type B という
副次解析対象の実施可能性を確保するための補完である。

したがって v1.2 の凍結作業（Phase 0 チェックリストの充足）は v1.3 の文言で継続してよい。
**ただし `v1.2-frozen` ではなく `v1.3-frozen` タグを使用すること。**
